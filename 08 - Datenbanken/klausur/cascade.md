# Cascade
## ON DELETE CASCADE
> Wenn in einer Parenttable `Besucher` ein Datensatz gelöscht werden soll:
> Wird in der besucht-Tabelle (Childtable) der Datensatz zu dem betroffenen Fremdschlüssel ebenfalls gelöscht.

## ON DELETE RESTRICT (oder NO ACTION)
